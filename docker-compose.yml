version: '3.8'

services:
  tpm-2-measurement-quote-engine:
    build: .
    container_name: tpm-2-measurement-quote-engine
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=tpm-2-measurement-quote-engine-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
